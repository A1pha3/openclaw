---
read_when:
  - 正在开发 Gateway 网关协议、客户端或传输层
summary: "Gateway 网关架构完整指南：组件、流程、协议和连接生命周期"
title: "Gateway 网关架构"
---

# Gateway 网关架构

## 🎯 学习目标

完成本文档学习后，你将能够：

### 基础目标（必掌握）

- [ ] 理解 OpenClaw 的整体架构设计
- [ ] 掌握 Gateway、客户端、节点之间的关系
- [ ] 理解 WebSocket 连接的生命周期
- [ ] 了解配对和本地信任机制

### 进阶目标（建议掌握）

- [ ] 理解线路协议的详细规范
- [ ] 掌握协议类型和代码生成流程
- [ ] 能够开发兼容的客户端
- [ ] 理解不变量和设计约束

---

## 💡 为什么需要 Gateway 架构？

### 类比：Gateway 就像"中央指挥部"

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Gateway 中央指挥部类比                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  现实场景：公司组织架构                                                 │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  员工（终端用户）───────► 中央指挥部（Gateway）◀─────► 各部门     │   │
│  │                           │                                  │   │
│  │                           ├─► 统一调度                        │   │
│  │                           ├─► 资源分配                        │   │
│  │                           ├─► 安全验证                        │   │
│  │                           └─► 状态监控                        │   │
│  │                                                                 │   │
│  │  好处：                                                          │   │
│  │  • 员工只需对接一个接口                                         │   │
│  │  • 资源高效共享，避免冗余                                       │   │
│  │  • 统一策略执行，便于管理                                       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  OpenClaw 场景：                                                       │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  用户（客户端）───────► Gateway 网关 ◀─────► 各渠道              │   │
│  │                           │                                  │   │
│  │                           ├─► 统一消息路由                    │   │
│  │                           ├─► 会话管理                        │   │
│  │                           ├─► 工具执行                        │   │
│  │                           └─► AI 代理调度                     │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 设计决策背景

```
┌─────────────────────────────────────────────────────────────────┐
│                    架构设计决策                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  问题：如何设计一个统一的 AI 代理网关系统？                      │
│                                                                 │
│  可选方案：                                                      │
│  1. 每个渠道独立运行 → 简单但无法统一管理                        │
│  2. 多个 Gateway 实例 → 复杂且资源浪费                          │
│  3. 单一 Gateway 网关（✓ 推荐方案）                             │
│                                                                 │
│  最终选择：方案 3                                               │
│  理由：                                                          │
│  ✅ 统一控制平面                                                │
│  ✅ 高效资源利用                                                │
│  ✅ 简化部署和管理                                              │
│  ✅ 安全的设备配对机制                                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 核心设计原则

| 原则 | 说明 | 好处 |
|------|------|------|
| **单一 Gateway** | 每台主机一个网关 | 避免 WhatsApp 会话冲突 |
| **WebSocket 优先** | 所有客户端通过 WS 连接 | 实时双向通信 |
| **类型化协议** | JSON Schema 验证所有消息 | 类型安全和可预测性 |
| **设备配对** | 新设备需要批准 | 安全的多设备访问 |

---

## 📋 核心概念

### 架构概览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           OpenClaw 架构概览                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        Gateway 网关（核心）                            │   │
│  │  ┌───────────────────────────────────────────────────────────────┐  │   │
│  │  │  • WebSocket 服务器（18789）                                   │  │   │
│  │  │  • HTTP API（RPC 接口）                                       │  │   │
│  │  │  • 消息渠道管理（WhatsApp/Telegram/Discord/...）              │  │   │
│  │  │  • 会话管理                                                    │  │   │
│  │  │  • 工具执行环境                                                │  │   │
│  │  │  • 事件分发（agent/chat/presence/health/heartbeat/cron）     │  │   │
│  │  └───────────────────────────────────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                    ┌───────────────┼───────────────┐                        │
│                    ▼               ▼               ▼                        │
│        ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐         │
│        │  控制平面客户端   │ │    WebChat      │ │   Canvas 主机   │         │
│        │  ┌───────────┐   │ │  ┌───────────┐  │ │  ┌───────────┐  │         │
│        │  │ macOS App │   │ │  │  Web UI   │  │ │  │ HTTP 文件 │  │         │
│        │  │ CLI       │   │ │  │  静态界面 │  │  │  │ 服务器   │  │         │
│        │  │ Web 管理界面│  │ │  └───────────┘  │ │  │ (18793)   │  │         │
│        │  └───────────┘  │ │                 │ │  └───────────┘  │         │
│        └─────────────────┘ └─────────────────┘ └─────────────────┘         │
│                                                                              │
│                    ┌───────────────┐                                        │
│                    ▼               ▼                                        │
│        ┌─────────────────────────────────────┐                             │
│        │           节点（Node）               │                             │
│        │  ┌───────────────────────────────┐ │                             │
│        │  │ role: node                     │ │                             │
│        │  │ 能力：canvas/camera/screen     │ │                             │
│        │  │ 命令：canvas.*/camera.* 等     │ │                             │
│        │  └───────────────────────────────┘ │                             │
│        └─────────────────────────────────────┘                             │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 组件详解

#### 1. Gateway 网关（守护进程）

**职责**：

| 功能 | 说明 |
|------|------|
| **维护提供商连接** | 管理 WhatsApp/Telegram 等渠道连接 |
| **WebSocket API** | 暴露类型化的 WS 接口 |
| **消息验证** | 根据 JSON Schema 验证入站帧 |
| **事件分发** | 发出 agent/chat/presence/health/heartbeat/cron 事件 |

#### 2. 控制平面客户端

**类型**：

| 客户端 | 用途 |
|--------|------|
| **macOS 应用** | 图形界面控制 |
| **CLI** | 命令行管理 |
| **Web 管理界面** | 浏览器控制 |

**特性**：
- 每个客户端一个 WS 连接
- 发送请求：`health`、`status`、`send`、`agent`、`system-presence`
- 订阅事件：`tick`、`agent`、`presence`、`shutdown`

#### 3. 节点（Node）

**角色定义**：

```javascript
// 节点连接时声明身份
{
  role: "node",
  capabilities: ["canvas", "camera", "screen", "location"],
  commands: ["canvas.*", "camera.*", "screen.record", "location.get"]
}
```

**特性**：
- 以 `role: node` 连接到同一个 WS 服务器
- 基于设备的配对机制
- 暴露设备专用命令

---

## 🔄 连接生命周期

### 单个客户端连接流程

```
Client                    Gateway
  │                          │
  │---- req:connect -------->│  1. 发送连接请求
  │                          │     （包含设备身份）
  │<------ res (ok) ---------│  2. 接收响应
  │   payload=hello-ok       │     （携带快照）
  │   (presence + health)    │
  │                          │
  │<------ event:presence ---│  3. 接收在线事件
  │<------ event:tick -------│  4. 接收心跳事件
  │                          │
  │------- req:agent ------->│  5. 发送 AI 请求
  │<------ res:agent --------│  6. 接收确认
  │   ack: {runId,           │     {runId, status: "accepted"}
  │         status:"accepted"}│
  │<------ event:agent ------│  7. 接收流式响应
  │   (streaming)            │
  │<------ res:agent --------│  8. 接收最终结果
  │   {runId, status,         │     {runId, status, summary}
  │    summary}               │
```

### 连接时序图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       WebSocket 连接时序                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  客户端                              Gateway                            │
│                                                                         │
│  1. 建立 WebSocket 连接                                                  │
│     ─────────────────────────────────────────────────────────────►     │
│                                                                         │
│  2. 发送 connect 消息                                                    │
│     {                                                                  │
│       type: "req",                                                      │
│       method: "connect",                                                │
│       params: {                                                         │
│         auth: { token: "..." },                                        │
│         identity: { deviceId: "..." }                                  │
│       }                                                                │
│     }                                                                  │
│     ─────────────────────────────────────────────────────────────►     │
│                                                                         │
│  3. 接收 connect 响应                                                  │
│     ◄───────────────────────────────────────────────────────────────  │
│     {                                                                  │
│       type: "res",                                                      │
│       payload: {                                                        │
│         presence: {...},                                               │
│         health: {...}                                                  │
│       }                                                                │
│     }                                                                  │
│                                                                         │
│  4. 接收事件流                                                         │
│     ◄──────── event:presence ────────────────────────────────────────  │
│     ◄──────── event:tick ────────────────────────────────────────────  │
│                                                                         │
│  5. 发送 agent 请求                                                    │
│     ─────────────────────────────────────────────────────────────►     │
│                                                                         │
│  6. 接收 agent 流式响应                                                 │
│     ◄───────────────────────────────────────────────────────────────  │
│     (多个 event:agent 消息)                                             │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔌 线路协议（Wire Protocol）

### 协议摘要

| 特性 | 说明 |
|------|------|
| **传输层** | WebSocket（文本帧） |
| **数据格式** | JSON 载荷 |
| **首帧要求** | **必须**是 `connect` |
| **认证** | `connect.params.auth.token` 必须匹配（如设置了令牌） |

### 请求格式

```json5
// 请求消息
{
  type: "req",
  id: "unique-request-id",
  method: "agent",
  params: { /* 方法参数 */ }
}
```

### 响应格式

```json5
// 响应消息
{
  type: "res",
  id: "unique-request-id",
  ok: true,
  payload: { /* 响应数据 */ }
}
```

### 事件格式

```json5
// 服务器推送事件
{
  type: "event",
  event: "agent",        // 事件名称
  payload: { /* 事件数据 */ },
  seq: 1,                // 序列号（可选）
  stateVersion: 1        // 状态版本（可选）
}
```

### 事件类型

| 事件 | 触发时机 | 载荷内容 |
|------|----------|----------|
| **agent** | AI 响应增量 | 流式文本块 |
| **chat** | 收到聊天消息 | 消息内容 |
| **presence** | 设备上线/下线 | 在线状态 |
| **health** | 健康状态变化 | 检查结果 |
| **heartbeat** | 定期心跳 | 时间戳 |
| **cron** | 定时任务触发 | 任务信息 |
| **shutdown** | Gateway 关闭 | 关闭原因 |
| **tick** | 定期系统状态 | 状态快照 |

### 幂等键（去重）

有副作用的方法（`send`、`agent`）需要幂等键以安全重试：

```json5
{
  type: "req",
  id: "req-id",
  method: "send",
  params: {
    idempotencyKey: "unique-key-for-retry",  // 幂等键
    // ... 其他参数
  }
}
```

---

## 🔐 配对与本地信任

### 设备身份流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                      设备配对与信任流程                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. 首次连接（新设备）                                                  │
│     ┌────────────────┐                                                   │
│     │  客户端发送    │                                                   │
│     │  connect 请求  │                                                   │
│     │  + 设备身份    │                                                   │
│     └────────┬───────┘                                                   │
│              │                                                           │
│              ▼                                                           │
│     ┌────────────────┐                                                   │
│     │ Gateway 检测   │                                                   │
│     │ 新设备 ID      │                                                   │
│     └────────┬───────┘                                                   │
│              │                                                           │
│              ▼                                                           │
│     ┌────────────────┐         ┌────────────────┐                        │
│     │ 本地连接       │  是     │ 自动批准       │                        │
│     │ (loopback)?    │────────►│ (流畅体验)     │                        │
│     └────────┬───────┘         └────────────────┘                        │
│              │否                                                        │
│              ▼                                                           │
│     ┌────────────────┐         ┌────────────────┐                        │
│     │ 非本地连接     │  是     │ 需要签名       │                        │
│     │ (远程)?        │────────►│ + 明确批准     │                        │
│     └────────┬───────┘         └────────────────┘                        │
│              │                                                           │
│              ▼                                                           │
│     ┌────────────────┐                                                   │
│     │ 颁发设备令牌   │                                                   │
│     │ 后续直接认证   │                                                   │
│     └────────────────┘                                                   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 信任级别

| 连接类型 | 验证要求 | 自动批准 |
|----------|----------|----------|
| **本地（loopback）** | 设备 ID | ✅ 是 |
| **本地（tailnet）** | 设备 ID | ✅ 是 |
| **远程** | 签名 + 明确批准 | ❌ 否 |

### 安全机制

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        多层安全机制                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  第 1 层：Gateway 认证（所有连接）                                     │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  • gateway.auth.token 验证                                      │   │
│  │  • 不匹配 → 立即关闭连接                                         │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                              ↓                                         │
│  第 2 层：设备身份（所有连接）                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  • connect.params.identity.deviceId                             │   │
│  │  • 新设备 → 配对流程                                             │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                              ↓                                         │
│  第 3 层：配对批准（远程连接）                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  • connect.challenge nonce 签名                                 │   │
│  │  • 用户明确批准                                                  │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                              ↓                                         │
│  第 4 层：设备令牌（后续连接）                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  • Gateway 颁发的设备令牌                                        │   │
│  │  • 无需重新批准                                                  │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🌐 远程访问

### 推荐方案：Tailscale/VPN

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Tailscale 网络拓扑                               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  你的 Mac                              Gateway 主机                    │
│  ┌────────────────┐                   ┌────────────────┐               │
│  │ Tailscale IP:  │                   │ Tailscale IP:  │               │
│  │ 100.x.y.z      │                   │ 100.x.a.b       │               │
│  └────────┬───────┘                   └────────┬───────┘               │
│           │                                     │                      │
│           └────────────── Tailscale VPN ───────┘                      │
│                          (加密隧道)                                   │
│                                                                         │
│  优势：                                                                  │
│  ✅ 无需手动端口转发                                                    │
│  ✅ 自动加密                                                            │
│  ✅ 内网穿透                                                            │
│  ✅ 同样的认证流程                                                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 替代方案：SSH 隧道

```bash
# 建立 SSH 隧道
ssh -N -L 18789:127.0.0.1:18789 user@gateway-host

# 本地连接到远程 Gateway
openclaw status --url ws://127.0.0.1:18789
```

### TLS 配置（可选）

```json5
{
  gateway: {
    bind: "tailnet",
    tls: {
      enabled: true,
      cert: "/path/to/cert.pem",
      key: "/path/to/key.pem"
    }
  }
}
```

---

## 📚 协议类型与代码生成

### 代码生成流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        协议代码生成流程                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. 定义模式（TypeBox）                                                 │
│     ┌─────────────────────────────────────────────────────────────────┐ │
│     │  const ConnectSchema = Type.Strict({                            │ │
│     │    auth: Type.Optional(AuthSchema),                             │ │
│     │    identity: Type.Optional(IdentitySchema),                     │ │
│     │  });                                                           │ │
│     └─────────────────────────────────────────────────────────────────┘ │
│                              ↓                                         │
│  2. 生成 JSON Schema                                                  │
│     ┌─────────────────────────────────────────────────────────────────┐ │
│     │  自动生成 JSON Schema 用于验证                                   │ │
│     └─────────────────────────────────────────────────────────────────┘ │
│                              ↓                                         │
│  3. 生成 Swift 模型                                                    │
│     ┌─────────────────────────────────────────────────────────────────┐ │
│     │  自动生成 macOS App 使用的 Swift 类型                            │ │
│     └─────────────────────────────────────────────────────────────────┘ │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## ⚙️ 操作快照

### 启动 Gateway

```bash
# 前台运行（开发模式）
openclaw gateway

# 带详细日志
openclaw gateway --verbose

# 指定端口
openclaw gateway --port 18789
```

### 健康检查

```bash
# 通过 CLI
openclaw health

# 通过 HTTP
curl http://127.0.0.1:18789/health
```

### 服务管理

```bash
# macOS
launchctl list | grep openclaw

# Linux
systemctl --user status openclaw-gateway
journalctl --user -u openclaw-gateway -f
```

---

## 🐛 故障排查

### 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 连接被拒绝 | `connect` 不是首帧 | 确保第一个消息是 `connect` |
| 认证失败 | Token 不匹配 | 检查 `gateway.auth.token` 配置 |
| 设备配对超时 | 远程连接未批准 | 通过 CLI 批准配对请求 |
| WebSocket 断开 | 网络问题或心跳超时 | 检查网络连接，增加心跳间隔 |

### 调试技巧

```bash
# 启用详细日志
openclaw gateway --verbose

# 查看连接状态
openclaw channels status --probe

# 测试 WebSocket 连接
wscat -c ws://127.0.0.1:18789

# 检查 Gateway 健康状态
curl http://127.0.0.1:18789/health
```

### 协议调试

```json5
// 有效 connect 消息示例
{
  "type": "req",
  "method": "connect",
  "id": "connect-1",
  "params": {
    "auth": {
      "token": "your-gateway-token"
    },
    "identity": {
      "deviceId": "my-client-001",
      "deviceName": "My Client",
      "clientType": "cli"
    },
    "subscribe": ["agent", "presence", "tick"],
    "protocol": "1.0"
  }
}
```

### 连接失败诊断流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       连接失败诊断流程                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  连接失败                                                                │
│     │                                                                   │
│     ▼                                                                   │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  错误类型？                                                      │   │
│  │                                                                 │   │
│  │  1. 连接超时/拒绝                                              │   │
│  │     ▼                                                           │   │
│  │     • 检查 Gateway 是否运行（openclaw health）                  │   │
│  │     • 检查端口是否正确（默认 18789）                            │   │
│  │     • 检查防火墙设置                                           │   │
│  │                                                                 │   │
│  │  2. 认证失败                                                    │   │
│  │     ▼                                                           │   │
│  │     • 检查 gateway.auth.token 是否匹配                          │   │
│  │     • 确认 token 格式正确                                       │   │
│  │                                                                 │   │
│  │  3. 设备未批准                                                  │   │
│  │     ▼                                                           │   │
│  │     • 等待配对批准或使用本地连接                                 │   │
│  │     • 检查 pending approvals                                    │   │
│  │                                                                 │   │
│  │  4. 协议错误                                                    │   │
│  │     ▼                                                           │   │
│  │     • 确保首帧是 connect                                        │   │
│  │     • 检查 JSON 格式是否有效                                    │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 最佳实践

### 客户端开发建议

| 实践 | 说明 |
|------|------|
| **处理异步事件** | 事件可能在响应之前到达 |
| **心跳重连** | 检测断开后自动重连 |
| **幂等性键** | 有副作用操作使用唯一键 |
| **状态版本** | 跟踪状态变化避免竞态 |

### 连接管理

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    连接生命周期管理建议                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  连接建立：                                                              │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  1. 建立 WebSocket 连接                                         │   │
│  │  2. 立即发送 connect 消息                                        │   │
│  │  3. 等待 hello-ok 响应                                          │   │
│  │  4. 开始处理事件流                                              │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  连接保持：                                                              │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  1. 监听 tick 心跳事件                                          │   │
│  │  2. 心跳超时 → 发送 ping                                        │   │
│  │  3. ping 超时 → 关闭并重连                                      │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  优雅断开：                                                              │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  1. 发送 close 消息                                             │   │
│  │  2. 等待服务器确认                                              │   │
│  │  3. 关闭 WebSocket 连接                                         │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  异常重连：                                                              │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  1. 指数退避（1s, 2s, 4s, 8s, 最大 30s）                        │   │
│  │  2. 记录断开原因                                                │   │
│  │  3. 重建连接后恢复状态                                          │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🚫 不变量（Invariants）

| 不变量 | 说明 | 违反后果 |
|--------|------|----------|
| **单一 Gateway** | 每台主机恰好一个 Gateway | WhatsApp 会话冲突 |
| **强制握手** | 首帧必须是 `connect` | 立即关闭连接 |
| **事件不重放** | 事件不会重传 | 客户端需刷新 |
| **设备认证** | 所有连接需要设备身份 | 安全风险 |

---

## 📚 相关文档

| 文档 | 链接 |
|------|------|
| **协议详细规范** | [Gateway 协议](/gateway/protocol) |
| **配对机制** | [配对](/channels/pairing) |
| **安全指南** | [安全](/gateway/security) |
| **系统架构** | [架构](/concepts/architecture) |

---

## 🎯 知识点回顾

| 技能 | 掌握程度 |
|------|----------|
| 理解 Gateway 架构 | ⭐⭐⭐⭐⭐ |
| 掌握连接生命周期 | ⭐⭐⭐⭐ |
| 理解配对机制 | ⭐⭐⭐⭐ |
| 开发兼容客户端 | ⭐⭐⭐ |

---

> **💡 专家提示**：开发客户端时，务必处理 `event` 流的异步特性——事件可能在响应之前到达！
