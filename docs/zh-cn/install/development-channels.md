---
summary: "开发渠道说明：stable、beta 和 dev 渠道的语义、切换方式和标签规则"
read_when:
  - 想要切换 stable/beta/dev 渠道
  - 想要使用最新开发版本
  - 了解版本发布策略
title: "开发渠道"
---

# 🔄 开发渠道

最后更新：2026-01-21

OpenClaw 提供三个更新渠道，满足不同用户的需求。

---

## 📊 渠道概览

| 渠道 | npm dist-tag | 稳定性 | 适用场景 |
|------|-------------|--------|---------|
| **stable** | `latest` | ⭐⭐⭐ 最稳定 | 日常使用，生产环境 |
| **beta** | `beta` | ⭐⭐ 测试中 | 尝鲜新功能，帮助测试 |
| **dev** | `dev` | ⭐ 最新 | 开发者，追踪最新代码 |

### 发布流程

```
开发者提交代码
       ↓
    main 分支（dev 渠道）
       ↓
    测试验证
       ↓
    beta 渠道发布
       ↓
    更多测试
       ↓
    stable 渠道发布
```

我们先将构建发布到 **beta**，测试后再**将经过验证的构建提升到 `latest`**——npm dist-tag 是 npm 安装的真正来源。

---

## 🔀 切换渠道

### Git 安装（源码）

```bash
# 切换到稳定版
openclaw update --channel stable

# 切换到测试版
openclaw update --channel beta

# 切换到开发版（main 分支）
openclaw update --channel dev
```

**各渠道行为**：
- `stable`/`beta`：检出最新匹配的标签
- `dev`：切换到 `main` 分支并 rebase 到上游

### npm/pnpm 安装（全局）

```bash
# 切换到稳定版
openclaw update --channel stable

# 切换到测试版
openclaw update --channel beta

# 切换到开发版
openclaw update --channel dev
```

通过对应的 npm dist-tag（`latest`、`beta`、`dev`）更新。

### 手动安装指定版本

```bash
# npm 安装特定 dist-tag
npm install -g openclaw@latest
npm install -g openclaw@beta
npm install -g openclaw@dev

# 安装特定版本号
npm install -g openclaw@2026.1.15
```

---

## 🔄 渠道切换时发生什么

当你使用 `--channel` **显式**切换渠道时，OpenClaw 会自动对齐安装方式：

| 目标渠道 | 安装方式 |
|---------|---------|
| `dev` | 确保有 git checkout（默认 `~/openclaw`），更新代码，从该目录安装全局 CLI |
| `stable`/`beta` | 使用对应 dist-tag 从 npm 安装 |

**自定义 Git 目录**：

```bash
export OPENCLAW_GIT_DIR=~/my-openclaw
openclaw update --channel dev
```

**并行运行多版本**：如果想同时运行 stable 和 dev，保持两个独立的克隆，将网关指向 stable 的那个。

---

## 🧩 插件与渠道

切换渠道时，OpenClaw 也会同步插件来源：

| 渠道 | 插件来源 |
|------|---------|
| `dev` | 优先使用 git checkout 中的打包插件 |
| `stable`/`beta` | 恢复 npm 安装的插件包 |

---

## 🏷️ 版本标签规则

### 版本号格式

```
vYYYY.M.D[-<修补>]

示例：
v2026.1.15      # 稳定版本
v2026.1.15-1    # 修补版本
v2026.1.15-beta.1  # 测试版本
```

### 标签最佳实践

| 规则 | 说明 |
|------|------|
| 标签不可变 | 永远不要移动或重用标签 |
| 匹配发布 | 只为希望 git checkout 落地的版本打标签 |
| dist-tag 为准 | npm dist-tag 是 npm 安装的真正来源 |

### dist-tag 映射

| dist-tag | 含义 |
|----------|------|
| `latest` | stable（稳定版） |
| `beta` | 候选构建 |
| `dev` | main 分支快照（可选发布） |

---

## 🍎 macOS App 可用性

**注意**：beta 和 dev 构建**可能不包含** macOS App 发布。

这没问题：
- git 标签和 npm dist-tag 仍然可以发布
- 发布说明或更新日志中会标注"此 beta 无 macOS 构建"

如果你需要 macOS App，建议使用 stable 渠道。

---

## 🤔 如何选择渠道

### 推荐 stable 的场景

- ✅ 日常个人使用
- ✅ 生产环境部署
- ✅ 希望稳定可靠
- ✅ 需要 macOS App

### 推荐 beta 的场景

- ✅ 想尝试即将发布的新功能
- ✅ 愿意帮助测试和报告问题
- ✅ 能接受偶尔的小问题

### 推荐 dev 的场景

- ✅ OpenClaw 开发者/贡献者
- ✅ 需要最新代码修复
- ✅ 想跟踪开发进度
- ⚠️ 可能遇到不稳定问题

---

## 🔧 常用命令

```bash
# 查看当前版本和渠道
openclaw --version
openclaw status

# 查看可用更新
openclaw update --check

# 更新到当前渠道最新版
openclaw update

# 切换渠道并更新
openclaw update --channel beta

# 安装特定版本（一次性）
openclaw update --tag v2026.1.15
```

---

## 📝 相关文档

- [更新指南](/zh-CN/install/updating) - 完整的更新流程
- [安装指南](/zh-CN/install) - 安装选项概览
- [更新日志](https://github.com/openclaw/openclaw/releases) - 版本发布历史
