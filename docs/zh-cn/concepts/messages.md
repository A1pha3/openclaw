---
summary: "消息系统详解 - 消息类型、处理流程和格式配置"
read_when:
  - 了解消息如何处理
  - 配置消息格式
  - 设置消息限制
title: "消息系统"
---

# 💬 消息系统

本文档详细介绍 OpenClaw 的消息系统，包括消息类型、处理流程和格式配置。

## 🎯 消息类型

### 文本消息

最基本的消息类型，支持 Markdown 格式：

| 格式 | 说明 | 示例 |
|------|------|------|
| **粗体** | `**文本**` | **粗体** |
| *斜体* | `*文本*` | *斜体* |
| ~~删除线~~ | `~~文本~~` | ~~删除线~~ |
| `代码` | `` `代码` `` | `代码` |
| 代码块 | ` ```语言\n代码\n``` ` | 多行代码 |

### 媒体消息

| 类型 | 支持渠道 | 说明 |
|------|----------|------|
| 图片 | 所有渠道 | JPEG、PNG、GIF、WebP |
| 音频 | 所有渠道 | MP3、WAV、OGG |
| 视频 | 大多数渠道 | MP4、MOV |
| 文件 | 所有渠道 | PDF、DOC、ZIP 等 |

### 特殊消息

| 类型 | 说明 |
|------|------|
| 位置分享 | 经纬度坐标 |
| 联系人 | 联系人卡片 |
| 投票 | Telegram、Discord 投票 |

---

## 📋 消息结构

### 入站消息

```
┌─────────────────────────────────────────┐
│  入站消息                                │
├─────────────────────────────────────────┤
│  id: "msg-123"                          │  消息 ID
│  channel: "whatsapp"                    │  来源渠道
│  type: "text" | "image" | "audio"       │  消息类型
│  from: "+15551234567"                   │  发送者
│  to: "+15559876543"                     │  接收者
│  content: "Hello!"                      │  文本内容
│  media: { /* 媒体信息 */ }               │  媒体附件
│  timestamp: 1704067200000               │  时间戳
│  metadata: { /* 元数据 */ }              │  额外信息
└─────────────────────────────────────────┘
```

### 出站消息

```
┌─────────────────────────────────────────┐
│  出站消息                                │
├─────────────────────────────────────────┤
│  id: "msg-456"                          │  消息 ID
│  channel: "whatsapp"                    │  目标渠道
│  type: "text" | "image" | "audio"       │  消息类型
│  to: "+15551234567"                     │  接收者
│  content: "Hello!"                      │  文本内容
│  media: { /* 媒体信息 */ }               │  媒体附件
│  replyTo: "msg-123"                     │  回复消息
│  formatting: { /* 格式 */ }              │  格式选项
└─────────────────────────────────────────┘
```

---

## 🔄 消息处理流程

```
用户发送消息
     │
     ▼
┌──────────────┐
│  渠道适配器  │ ─→ 验证消息格式
└──────────────┘
     │
     ▼
┌──────────────┐
│  消息队列    │ ─→ 消息缓冲
└──────────────┘
     │
     ▼
┌──────────────┐
│  路由引擎    │ ─→ 确定目标会话
└──────────────┘
     │
     ▼
┌──────────────┐
│  会话管理    │ ─→ 加载上下文
└──────────────┘
     │
     ▼
┌──────────────┐
│  AI 处理     │ ─→ 生成响应
└──────────────┘
     │
     ▼
┌──────────────┐
│  消息发送    │ ─→ 发送到渠道
└──────────────┘
```

---

## ⚙️ 消息配置

### 基础配置

```json5
{
  messages: {
    // 响应前缀
    responsePrefix: "🦞",  // 或 "auto" | "none"
    
    // 已读回执
    sendReadReceipts: true,
    
    // 输入提示
    typingIndicator: true
  }
}
```

### 队列配置

```json5
{
  messages: {
    queue: {
      mode: "collect",      // collect | immediate
      debounceMs: 1000,     // 消息收集时间
      cap: 20,              // 最大消息数
      drop: "summarize"     // summarize | oldest | newest
    }
  }
}
```

| 模式 | 说明 |
|------|------|
| `collect` | 收集多条消息后统一处理 |
| `immediate` | 立即处理每条消息 |

### 消息限制

```json5
{
  channels: {
    whatsapp: {
      textChunkLimit: 4000,  // 文本分块大小
      chunkMode: "length",   // length | sentence | paragraph
      mediaMaxMb: 50         // 媒体文件大小限制
    },
    telegram: {
      textChunkLimit: 4096,
      mediaMaxMb: 5
    }
  }
}
```

---

## 📝 消息格式

### Markdown 支持

OpenClaw 支持标准 Markdown 格式：

```markdown
# 标题 1
## 标题 2

**粗体文本**
*斜体文本*
~~删除线~~

- 列表项
1. 有序列表

`行内代码`

```javascript
// 代码块
function hello() {
  console.log("Hello!");
}
```

> 引用文本

[链接文本](https://example.com)
```

### 代码高亮

```json5
{
  messages: {
    code: {
      theme: "github-dark",
      showLineNumbers: true,
      maxLines: 100
    }
  }
}
```

### 格式选项

```json5
{
  messages: {
    formatting: {
      markdown: true,           // 启用 Markdown
      links: true,              // 自动链接
      mentions: true,           // @提及
      emoji: true,              // Emoji 支持
      codeHighlight: true       // 代码高亮
    }
  }
}
```

---

## 🔗 回复与引用

### 回复消息

```json5
{
  messages: {
    reply: {
      enabled: true,
      mode: "first"  // first | all | thread
    }
  }
}
```

| 模式 | 说明 |
|------|------|
| `first` | 回复原消息 |
| `all` | 回复所有相关消息 |
| `thread` | 在线程中回复 |

### 引用格式

```
> @用户名 原消息内容
>
> 回复内容
```

---

## ⏱️ 消息时间

### 时区配置

```json5
{
  agents: {
    defaults: {
      userTimezone: "Asia/Shanghai",  // 用户时区
      timeFormat: "auto"              // auto | 12 | 24
    }
  }
}
```

### 时间戳显示

```json5
{
  messages: {
    timestamps: {
      enabled: true,
      format: "relative",  // relative | absolute | disabled
      relative: {
        todayFormat: "HH:mm",       // 今天显示格式
        yesterday: "昨天 HH:mm",    // 昨天
        older: "MM-DD HH:mm"        // 更早
      }
    }
  }
}
```

---

## 🔔 消息通知

### 已读回执

```json5
{
  messages: {
    readReceipts: {
      enabled: true,
      scope: "all"  // all | none | dm
    }
  }
}
```

### 输入指示器

```json5
{
  messages: {
    typingIndicator: {
      enabled: true,
      timeoutMs: 5000
    }
  }
}
```

### 反应通知

```json5
{
  channels: {
    telegram: {
      reactionNotifications: "own"  // off | own | all
    },
    slack: {
      reactionNotifications: "own"
    }
  }
}
```

| 模式 | 说明 |
|------|------|
| `off` | 不显示反应 |
| `own` | 仅显示对自己的反应 |
| `all` | 显示所有反应 |

---

## 🛠️ 故障排除

### 消息发送失败

```bash
# 查看渠道状态
openclaw channels status

# 检查日志
openclaw logs --lines 100 | grep "message"

# 验证配置
openclaw config get channels
```

### 消息格式问题

```bash
# 测试 Markdown 渲染
openclaw message send --target "+15551234567" --message "**bold** *italic*"

# 检查格式配置
openclaw config get messages.formatting
```

### 消息延迟

```bash
# 检查队列状态
openclaw status | grep queue

# 查看队列配置
openclaw config get messages.queue
```

---

## 📊 消息统计

### 查看消息统计

```bash
# 消息统计
openclaw status --all | grep messages

# 查看会话消息数
openclaw sessions history <session-key> | wc -l
```

### 日志记录

```json5
{
  logging: {
    messages: {
      enabled: true,
      level: "info",
      excludeSensitive: true
    }
  }
}
```

---

## 📈 最佳实践

### 性能优化

```json5
{
  messages: {
    queue: {
      mode: "collect",
      debounceMs: 500,
      cap: 10
    },
    history: {
      maxMessages: 100,
      maxTokens: 50000
    }
  }
}
```

### 用户体验

```json5
{
  messages: {
    responsePrefix: "auto",  // 使用代理名称
    typingIndicator: true,   // 显示输入状态
    readReceipts: true       // 显示已读
  }
}
```

---

## 🔧 相关命令

| 命令 | 说明 |
|------|------|
| `openclaw message send` | 发送消息 |
| `openclaw message history` | 查看消息历史 |
| `openclaw sessions` | 管理会话 |
| `openclaw channels status` | 查看渠道状态 |

---

## 📚 相关文档

- [会话管理](/zh-CN/concepts/sessions) - 会话系统
- [消息路由](/zh-CN/concepts/routing) - 路由配置
- [群组消息](/zh-CN/concepts/group-messages) - 群组消息处理
- [CLI 参考](/zh-CN/cli) - 消息命令

---

**掌握消息系统，让沟通更高效！** 🦞
